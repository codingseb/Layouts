namespace CodingSeb.Layouts
{
    /// <summary>
    /// Define the way (unit in which) the distances properties (radius, center offsets, ...) are calculated in a RadialPanel
    /// </summary>
    public enum RadialPanelDistancesUnit
    {
        PercentageOfPanelSize,
        AbsolutePositioning
    }
}
